y = (1,2,3) # la tupla consta de varios elemnentos - la tupla es INMUTABLE no se pueden cambiar los elementos
print(y)
print (y [1])#muestreme el elemento en en indice 1 de esta tuple, muestra el 2
x = (1,) # la coma es para indicar que es una tupla de un solo elemento
print(x)
print(type(x)) # me muestra que es de tip tuple
print(x)#me da error puesto que ya habia borrado x
print(dir(x))
#del x # borre la tupla

#locatios =  {
#   (29.6, 25.2) : "Manizales",# tengo la clave y el valor de esta tupla
#   (28.6, 26.2) : "pereira"
#}